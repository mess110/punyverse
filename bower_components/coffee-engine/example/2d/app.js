// Generated by CoffeeScript 1.9.0
var GameScene, engine, scene,
  __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
  __hasProp = {}.hasOwnProperty;

engine = new Engine2D('game', 640, 480);

GameScene = (function(_super) {
  __extends(GameScene, _super);

  function GameScene() {
    return GameScene.__super__.constructor.apply(this, arguments);
  }

  GameScene.prototype.tick = function(tpf) {
    this.engine.clear();
    return this.drawText(Math.round(1 / tpf) + 'fps');
  };

  GameScene.prototype.doMouseEvent = function(event) {
    return console.log(event);
  };

  GameScene.prototype.doKeyboardEvent = function(event) {
    return console.log(event);
  };

  return GameScene;

})(BaseScene);

scene = new GameScene(engine);

SceneManager.get().addScene(scene);

SceneManager.get().setScene(scene);

engine.render();
